// constructing vectors
#include <iostream>
#include <vector>
using namespace std;

void getInput(){
	//get input
	//can be null or not, but must be valid. if we get invalid input that we can't convert into a job
	//we make the user give more input or do whatever signifies no new jobs for this tick

	//each valid job is inserted into the queue as it's parsed, using
	insert_job()
}

bool insert_job(job_description, n_procs, n_ticks){
	//attempts to insert a job. returns false if any of the information used to create the job is bad,
	//or if the insert failed for some other reason (no idea why it would)
}

int main (){  
	i = 0;
	while (true){
		i++;
	  scheduler.getInput()
	  if (i%20=0){
	  	scheduler.tick()
	  }
	  if (i=100){
	  	i=0;
	  }
	}
}